<%= content_for :title, 'New Jobs' %>
<div class="container mx-auto px-4 py-6">
  <h1 class="text-3xl font-bold mb-4 text-white">New Jobs</h1>
  <% if @jobs.present? %>
    <%= render "jobs", jobs: @jobs %>
  <% else %>
    <p class="text-gray-500">There are no new jobs.</p>
  <% end %>
</div>
